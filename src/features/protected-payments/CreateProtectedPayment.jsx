import PagePlaceholder from "../../components/ui/PagePlaceholder"

const CreateProtectedPayment = () => {
  return (
    <PagePlaceholder
      title="Create Protected Payment"
      description="Set up an escrow deal with seller, amount, deadline, and terms."
    />
  )
}

export default CreateProtectedPayment
